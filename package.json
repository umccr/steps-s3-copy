{
  "name": "steps-s3-copy",
  "private": true,
  "version": "0.0.0",
  "description": "The root package of a mono repo for Steps S3 copy",
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "dev",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter './packages/**' build",
    "watch": "bun run --filter './packages/**' build:watch",
    "package": "bun run --filter './packages/**' package",
    "clean": "git clean -fXd",
    "dev-deploy": "cd dev && bun run --cwd .. build && bunx cdk deploy",
    "dev-destroy": "cd dev && bun run --cwd .. build && bunx cdk destroy"
  },
  "keywords": [],
  "author": {
    "name": "University of Melbourne Centre for Cancer Research",
    "organization": true
  },
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/node22": "22.0.2",
    "@types/node": "22.15.26",
    "esbuild": "0.25.5",
    "prettier": "3.5.3",
    "typescript": "5.8.3"
  }
}
